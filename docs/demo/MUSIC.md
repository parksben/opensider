# Demo soundtrack

`opensider.mp4` 的背景床是本仓库用 ffmpeg `aevalsrc` 现场合成的短循环（踢鼓 / 踩镲 / 低音），没有采样任何受版权保护的录音。分镜见 `NEXT-TAKE.md`。

混音：相对画面压到约 0.28，头尾各淡入淡出 0.4–0.5 秒。画面里 Agent 流式段加速时，音乐仍按 1x 播放（不跟 setpts、不升调）。
