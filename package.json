{
  "name": "opensider",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "pnpm --filter @opensider/extension dev",
    "build": "pnpm --filter @opensider/extension build && node scripts/pack-extension.mjs",
    "pack-extension": "node scripts/pack-extension.mjs",
    "install-host": "node scripts/dev-host.mjs"
  }
}
